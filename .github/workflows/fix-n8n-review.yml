name: One-time n8n review fix

on:
  push:
    branches:
      - main

jobs:
  verify-trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "temporary workflow trigger is valid"
